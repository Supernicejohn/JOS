term.clear()
term.setCursorPos(1,1)
term.setTextColor(colors.blue)
term.setBackgroundColor(colors.black)
print("JOS system X")
term.setTextColor(colors.red)
print("Note: this version is designed to be ran on top of CraftOS bios")
print("If ran on a custom bios, the OS may not work")
term.setTextColor(colors.blue)
print()
print("Starting OS")
shell.run("JOS1/system/kernel.xsf")
